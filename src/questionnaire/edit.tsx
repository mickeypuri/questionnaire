import { iEdit } from "./interfaces/iEdit";

const Edit = ({
    answer,
    question,
    handleEditUpdate
}: iEdit) => {

    return (
        <div>ToDo: Edit Response to a single Question</div>
    )

};

export default Edit;